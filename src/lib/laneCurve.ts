//Lane Lab constants shared by the panels and the hero build pages: the game-start souls, the
//league sample floor and the tail-guard fraction.

export const SOULS_AT_ZERO = 600;

export const RANK_MIN_SAMPLE = 500;

export const MIN_SAMPLE_FRACTION = 0.05;
