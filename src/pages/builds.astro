---
//Legacy route: early deployments linked /builds in the nav; the content lives
//on the /build-lab page now. A server-side 301 folds old links and crawl
//entries into it.
export const prerender = false;
return Astro.redirect('/build-lab', 301);
---
